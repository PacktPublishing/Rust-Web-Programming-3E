[package]
name = "core-system"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4.5.16", features = ["derive"] }
to-do-dal = { path = "../dal" }

[features]
json-file-storage = ["to-do-dal/json-file"]
default = ["json-file-storage"]
